Sample OGS project for launcher UI testing (sealed-style mode).

Use this folder with the Projects page to validate:
- force_offline=true behavior in ogs_config.json
- sealed-style tool path layout produced by seal workflow

This sample contains placeholder files for tool binaries and is not intended
for launching real tools.
